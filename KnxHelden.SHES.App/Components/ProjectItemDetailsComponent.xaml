<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="KnxHelden.SHES.App.Components.ProjectItemDetailsComponent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KnxHelden.SHES.App.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:KnxHelden.SHES.Controls.FormFieldService"
    xmlns:validation="using:WinUIValidation"
    mc:Ignorable="d">

    <validation:Form Model="{x:Bind ViewModel}">
        <GridView ItemsSource="{x:Bind ViewModel.FormFields}" SelectionMode="None">
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="2"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>

            <GridView.ItemTemplate>
                <DataTemplate x:DataType="controls:FormField">
                    <Grid MinHeight="50" Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="250" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="{x:Bind Label}" FontWeight="Bold" VerticalAlignment="Center" />
                        <ContentPresenter Grid.Column="1"
                                  VerticalAlignment="Center"
                                  Content="{x:Bind Control}" />
                        <validation:ValidationFor Grid.Column="1"
                                                  Grid.Row="1"
                                                  PropertyName="{x:Bind PropertyName}" Margin="0,5,0,0" />
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </validation:Form>
</UserControl>

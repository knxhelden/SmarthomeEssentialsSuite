<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="KnxHelden.SHES.App.Views.StructureView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KnxHelden.SHES.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:components="using:KnxHelden.SHES.App.Components"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:BoolNegationConverter x:Key="BoolNegationConverter"/>
    </Page.Resources>

    <Grid VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="200" Width="300" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition MinWidth="300" Width="*" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition MinWidth="200" Width="300" />
        </Grid.ColumnDefinitions>

        <!-- PROJECT TREE -->
        <Grid Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
              VerticalAlignment="Stretch"
              Padding="16">
            <components:ProjectTreeCompoment x:Name="ProjectTreeCompoment" VerticalAlignment="Stretch" />
        </Grid>

        <controls:GridSplitter Grid.Column="1"
                               Background="{ThemeResource SmokeFillColorDefaultBrush}" />

        <!-- PROJECT ITEM TABS -->
        <Grid Grid.Column="2"
              VerticalAlignment="Stretch"
              Margin="16,0,16,0">
            <TabView IsAddTabButtonVisible="False"
                     TabWidthMode="SizeToContent">
                <!-- DEVICES TAB -->
                <TabViewItem x:Uid="StructureView_DevicesTab"
                 IsClosable="False"
                 Visibility="{x:Bind ViewModel.LocationTabVisibility, Mode=OneWay}"
                 IsSelected="{x:Bind ViewModel.LocationTabSelected, Mode=OneWay}">
                    <TabViewItem.IconSource>
                        <FontIconSource FontFamily="{StaticResource MaterialDesignIcons}" Glyph="&#xF12E4;" />
                    </TabViewItem.IconSource>

                    <components:ProjectItemDevicesComponent x:Name="ProjectItemDevicesComponent" Margin="0,12,0,0" />
                </TabViewItem>
                <!-- CABINET TAB -->
                <TabViewItem x:Uid="StructureView_CabinetTab"
                 IsClosable="False"
                 Visibility="{x:Bind ViewModel.CabinetTabVisibility, Mode=OneWay}">
                    <TabViewItem.IconSource>
                        <FontIconSource FontFamily="{StaticResource MaterialDesignIcons}" Glyph="&#xF12E4;" />
                    </TabViewItem.IconSource>

                    <components:ProjectItemCabinetComponent x:Name="ProjectItemCabinetComponent" />
                </TabViewItem>
                <!-- DEVICE TAB -->
                <TabViewItem x:Uid="StructureView_DetailsTab"
                 IsClosable="False"
                 Visibility="{x:Bind ViewModel.DetailsTabVisibility, Mode=OneWay}"
                 IsSelected="{x:Bind ViewModel.LocationTabSelected, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}">
                    <TabViewItem.IconSource>
                        <FontIconSource FontFamily="{StaticResource MaterialDesignIcons}" Glyph="&#xF12E4;" />
                    </TabViewItem.IconSource>

                    <components:ProjectItemDetailsComponent x:Name="ProjectItemDetailsComponent" />
                </TabViewItem>
            </TabView>
        </Grid>

        <controls:GridSplitter Grid.Column="3"
                               Background="{ThemeResource SmokeFillColorDefaultBrush}" />

        <!-- PROJECT ITEM METADATA -->
        <Grid Grid.Column="4"
              VerticalAlignment="Stretch"
              Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
              Padding="16">
            <components:ProjectItemMetadataComponent x:Name="ProjectItemMetadataComponent" />
        </Grid>
    </Grid>
</Page>
